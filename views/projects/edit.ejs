<%- include('../partials/headers'); -%>

<div class="container container-card" style="margin-top: 80px;">
    <h1>Edit Project</h1>
    <form action="" method="POST" id="form-siswa">
        <div class="form-group">
            <label for="exampleInputEmail1">Name</label>
            <input type="string" class="form-control" id="projectname" aria-describedby="emailHelp" name="projectname"
                value="<%= nameProject.name%>">
        </div>
        <fieldset class="form-group">
            <div class="row">
                <legend class="col-form-label col-sm-2 pt-0">Member</legend>
                <div class="col-sm-10">
                    <% members.forEach ((item,index) => { %>
                    <div class="form-check form-check-inline">
                        <input class="form-check-input" type="checkbox" name="editmembers" id="members<%= index + 1%>"
                            value="<%= item.userid %>">
                        <label class="form-check-label" for="members<%= index + 1 %>">
                            <%= item.fullname %>
                        </label>
                    </div>

                    <br><br>
                    <% }) %>
                </div>
            </div>
        </fieldset>
        <button type="submit" class="btn btn-primary">Update</button>
    </form>
</div>